# Khicas CG50 开发与验收工作流

## 职责与停止点

- 主代理负责数学审查、源码分析、修复、测试、验收、性能与资源检查、文档、Git 和 checkpoint。
- 常规 subagent 只出题。每次只启动一个全新代理，不 follow-up、不复用上下文；交付后结束。
- 首轮新题存在产品失败：主代理修复、复验并保存 checkpoint，再派发全新题集。修复后通过不能代替新批次首次通过。
- 一整批有效新题首次全过：停下来向用户简报，按后续指示继续。此后才允许派发一个全新的最终性能审查代理。
- 最终审查代理只分析性能、栈、分配/拷贝和冗余；主代理实现、验收并提交 final checkpoint。发现新正确性问题则返回出题修复流程。
- 用户当前指定的任务边界优先。**任何有歧义或模糊的要求，必须先向用户确认；确认前不得执行相关操作，不得自行补充授权。** 已明确且独立的工作可以继续。

## 出题与验证

1. 阅读已有输入、覆盖目录和公开缺口，先列覆盖矩阵。每批以 **40 道以上实质不同的新题**为目标，数学质量优先；不足时说明缺少的方向，不靠换常数或近似重复凑数。
2. 覆盖积分、导数、化简、普通/参数/极坐标双向转换，以及极限、和积、矩阵、精确算术和有界资源压力。优先尚未覆盖的结构。
3. 同时给出有效输入、边界、参数退化和不应套用规则的反例。写明 ID、可输入表达式、条件、答案/期望行为、证明、原始域/分支、新意、优先级；预算标为要求，不能冒充实测。
4. 主代理独立审查参考答案，不从程序输出倒推答案，不硬编码题目。保留当前批次首测与复测；不得删失败题、改答案或降低精度制造通过。
5. 优先处理 Address/TLB、错误答案和域/分支丢失，再处理未求出、显示和性能。数值抽样只代表采样一致；非初等特殊函数可算求出，保留积分不算求出。
6. 不定积分验证原域上的导数，定积分检查收敛与端点；区分普通反常积分和主值。转换检查代回、完整像集、遗漏点、参数区间及半径/角度约束。

## 代码要求

- 生产源码只放 `src/`：计算内核 `cas/`、界面 `ui/`、设备适配 `platform/`、运行库 `runtime/`、生成文件 `generated/`。构建、资产、预编译依赖分别放 `build/`、`assets/`、`vendor/`。
- 代码、测试和文档只维护当前有效内容，不为上一版本保留副本或过渡实现；能精简就精简，能删除就删除，保留核心功能、必要依赖和有效验证。历史由 Git 保存。
- 优先复用已有函数和数据结构；共享同一不变量的逻辑，避免复制规则、并行旧实现、字符串拼接解析表达式和无调用代码。
- 函数职责单一，命名说明用途；使用清晰缩进、空格和必要空行。C/C++ 遵循 `.clang-format`，Python 遵循四空格缩进。不要格式化生成表或纯许可证文本。
- 注释说明数学条件、边界、所有权和非显然的不变量，不记录对话、取舍过程或 checkpoint 历史。
- 明确常量引用、值传递与对象所有权；避免不必要的容器复制、重复归一化和临时分配。不要保存可能因扩容、递归或上下文变化而失效的引用。
- 索引使用前检查边界；明确空容器、退化矩阵、零分母、溢出、别名和失败返回值。查找失败不得留下可误用的有效索引。
- 符号递归、展开、匹配、阶数和输出规模必须有合理上限；检查失败时保持可停止并保留原式，不能因资源保护返回错误答案。
- 区分实数根、复数主分支和未知假设；不得擅自取消原域空点或求值本应惰性的条件分支。参数题必须处理零值、符号及收敛条件。
- 不降低精度、放宽容差或开启 `fast-math` 换取通过。汇编必须匹配 CG50 的 SH4 架构，提供可维护的接口及与原算法一致的验证。
- 改动构建路径时检查所有输入、相对链接、生成步骤和主机探针；不得静默链接旧实现、错误平台头文件或旧安装包。
- 测试代码和生产代码分离。**不添加或执行无意义的 smoke test。仅在核心修改关联到重点行为时，选择必要、最少的测试**，覆盖直接受影响的数学恒等式、边界或拒绝条件；不增加只复述实现的测试，不因整理文档而运行无关回归。
- 必要检查通过后停止扩测和重复测试；只有新改动、失败或尚未解决的直接风险才追加验证。用户明确要求的专项验收仍须完成。

## 测量与资源

- 保留准确错误类型：Address、TLB、主机信号和超时分开；未经定位，不认定同一根因。
- 使用实际源码探针，分别测普通栈和受保护小栈；64 KiB 主机栈不是 SH4/MMU 模拟，RSS 不是 CG50 RAM。
- 性能使用可追溯源码/探针哈希、相同构建条件和交错重复测量，报告具体样例、基线及限制，不推算整机倍率。
- 容量以真实 SH4 链接和资源报告为准，不扩大固定代码区、侵占 CAS 堆或把旧产物冒充新构建。不把暂未实现或链接超限宣称为理论上不可能。

## 文档与报告

- README 仅描述当前功能、使用和构建入口，不写讨论、决策过程或逐轮历史。
- `docs/` 按固定职责维护：`architecture.md`、`changelog.md`、`passed-tests.md`、`tests-set.md` 和 `bench/`。未经用户要求不新增文档；优先更新现有文件。
- `docs/bench/` 固定使用 `acceptance.json`、`regression.json`、`verification.json`、`resources.json`、`performance.json`。更新同名文件，不在文件名中添加版本、日期或轮次。
- 文档只保留最新状态；历史依赖 Git。测试 ID 可稳定保留，不能将不同实质题目当作历史垃圾删除。
- **所有文档和发布报告只在提交/checkpoint 前集中更新。** 工作中的题目、原始运行数据和草稿放 `tests/`、`.build/` 或临时目录，不持续改写 docs。
- 汇报适合快速扫描：先给结果，再给少量并列事项。RCA 直接给“来源 → 调用链 → 后果”；修复直接给方案和验证。省略讨论过程、决策点及重复背景。

## Git、checkpoint 与设备

- 所有新提交和 checkpoint 标签使用用户配置的 **OpenPGP** 签名；禁止关闭签名或无签名重试。使用 `.githooks/`，推送/PR 前验证待发布提交和标签。
- checkpoint 包含必要代码、测试、当前通过/失败与资源状态；完成 `git diff --check`、相关回归、签名检查及适用的交叉构建后再提交。未完成验收不能标为 final。
- `dist/`、`.build/`、本地缓存和编辑器临时文件必须忽略；恢复引用不推送。
- 安装先确认 CASIO 设备及包的来源、尺寸和哈希，不触碰其他磁盘；完成写入校验并安全关闭存储连接。屏幕提示“不要拔下 USB”时不要直接拔线。

## 接续入口

读取当前 Git 状态及提交/tag、`docs/changelog.md`、`docs/tests-set.md` 和 `docs/bench/`。以实际源码、验证记录和设备校验为准。

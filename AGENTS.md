# Khicas CG50 开发与验收工作流

本文件记录用户明确要求的长期工作流，适用于本仓库。后续会话开始时先读取本文件、Git 状态和最新 checkpoint 文档，接续未完成的工作，不从头重复。用户在当前会话中的更新要求优先。

## 职责和代理生命周期

- 主代理负责数学审查、源码分析、修复、测试实现、验收、性能测量、资源检查、文档和 checkpoint。
- 常规 subagent **只负责出题**，不得修改生产代码、替主代理修复或承担验收。
- 每轮只启动一个全新的出题 subagent；不要同时派发大量代理。不要通过 follow-up 复用旧代理及其上下文。
- 题集交付后结束该代理；若工具仅支持 interrupt，则显式 interrupt 并不再复用。不要声称已删除平台不支持删除的上下文。
- 出题阶段不要派发最终性能审查代理。主代理为修复和容量检查自行分析性能不受此限制。

## 出题要求：广泛覆盖尚未实现的能力

1. 先阅读已有题集的输入、覆盖目录和公开缺口，整理覆盖矩阵。出题可以利用已知缺口，但不要依据当前程序的输出倒推答案或构造只会通过的题。
2. 每轮应交付广泛的题集，通常以 **40–80 道实质不同的新题**为目标；数量服从数学质量。若无法提供足量有效新题，要列明缺少的覆盖方向，不要靠换常数、交换变量或近似重复凑数，也不要静默退化为几道样题。
3. 覆盖积分、导数、化简、方程转换及相关核心计算，优先现有题集未覆盖的结构。可以分文件组织同一批次，但所有子集共同决定该批次是否首次全过。
4. 尽可能同时覆盖：
   - 积分：不定/定积分、参数、有限/无穷区间、根式、超越函数、特殊函数、对称性、条件收敛、普通反常积分与主值的区别。
   - 导数：复合函数、参数退化、绝对值、分段和 min/max 接触、不可导点、端点与孤立定义点。
   - 化简：等式、实/复分支、对数与根式、分母原有空点、条件表达式，以及嵌套、重复子式和表达式膨胀。
   - 方程转换：普通/参数/极坐标双向转换，圆锥曲线、奇异曲线、多分支、参数遗漏点、退化参数、角度和半径约束、结果代回与完整像集。
   - 其他核心能力：求和/乘积、极限、矩阵/行列式、精确算术及实际可复现的资源压力场景。
5. 同一结构同时给出有效输入、边界/退化情况和不应套用规则的反例。资源题保持有界、可停止，区分数学上无解、非初等、发散、未实现和资源不足。
6. 每道题写明：唯一 ID、可输入的 Khicas 表达式、变量及参数条件、正确答案或可验证的期望行为、证明思路、原始定义域/分支、覆盖新意、相关反例、优先级。题目要求的资源预算必须标为要求，不得冒充实测结果。
7. 主代理独立审核参考答案。不能把出题代理的答案直接当作真值；有疑问时先证明、求导或独立高精度核验，再判断产品缺陷。

## 迭代与进入最终审查的条件

1. 分析当前所有未解决题目和用户报告，优先 Address/TLB 等崩溃、错误答案、分支/定义域丢失，再处理未求出的结果、显示和性能。
2. 主代理修复通用结构，而不是硬编码题目答案。分析整个适用域、退化参数、错误套用风险、递归深度、数据拷贝、表达式膨胀和求值链。
3. 保存新题**第一次运行**的原始结果。后续修复后的结果另存，不能覆盖失败历史，不能通过删题、改答案或降低标准制造全过。
4. 修复后执行独立数学验证、必要的反例/边界测试、相关旧回归和资源检查；更新通过题目列表及缺口，创建 checkpoint。
5. **只要本批新题首轮存在产品失败项，即使全部修复且复验通过，也必须另启一个全新出题 subagent，开始下一批。此时不得派发最终性能审查代理。**
6. 只有一整批足够广泛、实质新增的有效题目在**首次验收**中全部通过，才允许启动一个全新的最终性能审查 subagent。
7. 最终审查代理仅分析性能、栈、分配/拷贝、冗余与可清理代码等，返回证据和建议；实现、数学正确性和验收仍由主代理完成。交付后同样结束且不复用。
8. 主代理处理审查结论，验证后提交 final checkpoint。若期间发现新的正确性或崩溃问题，先修复并重新进入出题验收循环，不能沿用之前的全过结论。

## 验收和性能证据

- 不定积分验证原始定义域上的导数；定积分同时检查收敛和端点；等式转换同时验证代回与分支完整性。数值抽样只能证明抽样一致，不能代替符号或域覆盖证明。
- 对崩溃报告保留用户提供的准确错误类型。Address error 和 TLB error 不混写；主机超时、崩溃和设备错误分开记录，未定位时不认定同一根因。
- 主机验证优先使用仓库已有的真实源码探针；普通栈和受保护的小栈分别测试。64 KiB 主机保护栈不是 SH4/MMU 模拟器，主机 RSS 不是 CG50 RAM。
- 比较性能时使用可追溯的源码/探针哈希、相同构建条件、交错重复测量；注明测试对象、基线和限制。不要把一次耗时、超时下界或主机倍率表述为整机提速。
- 容量必须由真实 SH4 链接和资源报告确认。不得为通过检查扩大固定 ROM/AC2 区域或侵占 CAS 堆，也不能在链接溢出时发布旧产物冒充新包。
- 非初等积分允许有定义且分支正确的特殊函数答案；保留原积分表示尚未求出，不能计为“已求出”。不能把暂时未实现或链接超限直接宣称为理论上不可能。
- 独立通过列表维护在 `docs/PASSED-INTEGRALS.md`；新增能力、失败历史、验收证据和逐项缺口维护在相应 checkpoint 文档及 `docs/benchmarks/`。开发中通过但尚未打包的状态须单独标明。

## Checkpoint、推送和设备

- Checkpoint 应包含代码、必要测试、数学与资源验证、通过/未通过清单和适用限制。提交前确认 `git diff --check`、相关回归及交叉构建结果；不能将未完成验收标记为 final。
- 用户明确授权过的动作无需重复征求同意，但不能绕过实际的自动审批拒绝。遇到拒绝，记录具体动作和理由，完成不受影响的工作，再按当前授权和工具规则处理。
- 安装前识别真正的 CASIO 设备，核验包的来源、尺寸和哈希；不得触碰其他磁盘。安装后完成写入和校验、安全关闭存储连接；设备显示“不要拔下 USB”时不要要求直接拔线。
- 最近用户对 checkpoint 替换的偏好是**不额外备份，直接替换上一 checkpoint**；这不授权清理其他磁盘或无关文件。
- 新会话重新核实设备是否连接、安装版本和远端状态。不要把历史 USB 路径、一次会话的安装/推送许可或曾经的通过状态当作已完成的新动作。

## 当前进度的查找入口

先查看最近的 Git 提交/tag、`docs/POLAR-STABILITY.md`、`docs/CALCULUS-CORE-AUDIT.md` 和最新 benchmark。Address 报告与开发中修复见 `docs/ADDRESS-ERROR-INVESTIGATION.md`。状态以实际源码、报告和设备校验为准，不以本文件中的工作流替代验收。

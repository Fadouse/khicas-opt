{
  "runs": [
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "normal",
      "point": "pi",
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "normal",
      "point": "pi",
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "normal",
      "point": "pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "normal",
      "point": "pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "normal",
      "point": "-pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "normal",
      "point": "-pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(2*sin(x^2)*abs(sin(x))+x,x)",
      "stack": "normal",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(2*sin(x^2)*abs(sin(x))+x,x))",
      "stack": "normal",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(2*sin(x^2)*abs(sin(x))+x,x)",
      "stack": "normal",
      "point": "pi",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(2*sin(x^2)*abs(sin(x))+x,x))",
      "stack": "normal",
      "point": "pi",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(cos(x)*abs(sin(2*x))+x,x)",
      "stack": "normal",
      "point": "pi/2",
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(cos(x)*abs(sin(2*x))+x,x))",
      "stack": "normal",
      "point": "pi/2",
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(cos(x)*abs(sin(2*x))+x,x)",
      "stack": "normal",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(cos(x)*abs(sin(2*x))+x,x))",
      "stack": "normal",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(exp(x)*abs(sin(3*x))+x,x)",
      "stack": "normal",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(exp(x)*abs(sin(3*x))+x,x))",
      "stack": "normal",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(exp(x^2)*abs(cos(2*x)),x)",
      "stack": "normal",
      "point": "pi/4",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(exp(x^2)*abs(cos(2*x)),x))",
      "stack": "normal",
      "point": "pi/4",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "64",
      "point": "pi",
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "64",
      "point": "pi",
      "result": "0",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "64",
      "point": "pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "64",
      "point": "pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(sin(x)*abs(sin(2*x))+cos(x),x)",
      "stack": "64",
      "point": "-pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(sin(x)*abs(sin(2*x))+cos(x),x))",
      "stack": "64",
      "point": "-pi/2",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(2*sin(x^2)*abs(sin(x))+x,x)",
      "stack": "64",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(2*sin(x^2)*abs(sin(x))+x,x))",
      "stack": "64",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(2*sin(x^2)*abs(sin(x))+x,x)",
      "stack": "64",
      "point": "pi",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(2*sin(x^2)*abs(sin(x))+x,x))",
      "stack": "64",
      "point": "pi",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(cos(x)*abs(sin(2*x))+x,x)",
      "stack": "64",
      "point": "pi/2",
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(cos(x)*abs(sin(2*x))+x,x))",
      "stack": "64",
      "point": "pi/2",
      "result": "1",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(cos(x)*abs(sin(2*x))+x,x)",
      "stack": "64",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(cos(x)*abs(sin(2*x))+x,x))",
      "stack": "64",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(exp(x)*abs(sin(3*x))+x,x)",
      "stack": "64",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(exp(x)*abs(sin(3*x))+x,x))",
      "stack": "64",
      "point": 0,
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "diff(exp(x^2)*abs(cos(2*x)),x)",
      "stack": "64",
      "point": "pi/4",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    },
    {
      "input": "simplify(diff(exp(x^2)*abs(cos(2*x)),x))",
      "stack": "64",
      "point": "pi/4",
      "result": "undef",
      "exact": true,
      "method": "At a simple sine/cosine zero, analytic amplitude zero makes the original quotient tend to zero; nonzero amplitude gives opposite slopes. Piecewise derivatives require both adjacent quotients relative to the actual selected value; shadowed equality branches do not change it. Oscillatory x\u00b2 sin(1/x)=O(x\u00b2) has removable slope zero only if the selected point value is zero."
    }
  ]
}

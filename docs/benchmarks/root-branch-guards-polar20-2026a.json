{
  "runs": [
    {
      "input": "diff(surd((x-1)^4,3),x)",
      "stack": "normal",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd((x-1)^4,3),x))",
      "stack": "normal",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^5,3),x)",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^5,3),x))",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^3*(x-1)^5,3),x)",
      "stack": "normal",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^3*(x-1)^5,3),x))",
      "stack": "normal",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^3*(x-1)^4,3)/(1+x^2)+sin(x),x)",
      "stack": "normal",
      "point": 1,
      "result": "cos(1)",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^3*(x-1)^4,3)/(1+x^2)+sin(x),x))",
      "stack": "normal",
      "point": 1,
      "result": "cos(1)",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<0,exp(x)-1,ln(1+x)),x)",
      "stack": "normal",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<0,exp(x)-1,ln(1+x)),x))",
      "stack": "normal",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)),x)",
      "stack": "normal",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)),x))",
      "stack": "normal",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)-1),x)",
      "stack": "normal",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)-1),x))",
      "stack": "normal",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+(exp(x-1)-1)/2),x)",
      "stack": "normal",
      "point": 1,
      "result": "1/2",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+(exp(x-1)-1)/2),x))",
      "stack": "normal",
      "point": 1,
      "result": "1/2",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt((x+i)/(x-i))",
      "stack": "normal",
      "point": 0,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt((x+i)/(x-i)))",
      "stack": "normal",
      "point": 0,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt((x-2+i)/(x-2-i))",
      "stack": "normal",
      "point": 2,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt((x-2+i)/(x-2-i)))",
      "stack": "normal",
      "point": 2,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt(x+i*x)",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt(x+i*x))",
      "stack": "normal",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd((x-1)^4,3),x)",
      "stack": "64",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd((x-1)^4,3),x))",
      "stack": "64",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^5,3),x)",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^5,3),x))",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^3*(x-1)^5,3),x)",
      "stack": "64",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^3*(x-1)^5,3),x))",
      "stack": "64",
      "point": 1,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(surd(x^3*(x-1)^4,3)/(1+x^2)+sin(x),x)",
      "stack": "64",
      "point": 1,
      "result": "cos(1)",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(surd(x^3*(x-1)^4,3)/(1+x^2)+sin(x),x))",
      "stack": "64",
      "point": 1,
      "result": "cos(1)",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<0,exp(x)-1,ln(1+x)),x)",
      "stack": "64",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<0,exp(x)-1,ln(1+x)),x))",
      "stack": "64",
      "point": 0,
      "result": "1",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)),x)",
      "stack": "64",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)),x))",
      "stack": "64",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)-1),x)",
      "stack": "64",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+exp(x-1)-1),x))",
      "stack": "64",
      "point": 1,
      "result": "undef",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "diff(piecewise(x<=1,ln(1+x),ln(2)+(exp(x-1)-1)/2),x)",
      "stack": "64",
      "point": 1,
      "result": "1/2",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(diff(piecewise(x<=1,ln(1+x),ln(2)+(exp(x-1)-1)/2),x))",
      "stack": "64",
      "point": 1,
      "result": "1/2",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt((x+i)/(x-i))",
      "stack": "64",
      "point": 0,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt((x+i)/(x-i)))",
      "stack": "64",
      "point": 0,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt((x-2+i)/(x-2-i))",
      "stack": "64",
      "point": 2,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt((x-2+i)/(x-2-i)))",
      "stack": "64",
      "point": 2,
      "result": "i",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "sqrt(x+i*x)",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    },
    {
      "input": "simplify(sqrt(x+i*x))",
      "stack": "64",
      "point": 0,
      "result": "0",
      "exact": true,
      "method": "Root power exponent >1 gives a zero original difference quotient. Analytic exponential/logarithm branches require equality of both values and slopes; common log(2) cancels exactly. Principal complex roots at the cut are evaluated on the original radicand (-1 or 0)."
    }
  ]
}

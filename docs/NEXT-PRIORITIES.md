# 当前排期

1. 用户 A1–F6 已有36/36全模式通过，D1–D4含显式复参数条件已通过，F5与Q6的Li2已实现求值/微分；两批出题代理均8/8通过。三角对数不定积分再新增14道全模式通过，含原来报告的3*ln(sin(2*x))。此前另外5道错题已经全部精确通过，保留为回归。
2. 本轮已修复atan在零点新增除零点，以及化简中间向量类型访问；大导数、混合表达式及高次公共因子的进一步压缩仍受保护预算限制，记录为后续扩展。
3. 当前转入用户要求的方程转换优化：圆、椭圆、双曲线的分支覆盖，圆锥曲线识别优先级，以及 `x(t)=…、y(t)=…` 标签丢失。

方程转换验收应包含：把参数式回代隐式方程验证恒等为零；单独检查实分支覆盖与遗漏点；用圆的案例排除 `x=t,y=t` 之类错误解。仅回代成功不足以证明覆盖完整。此前显式函数、隐式直线、抛物线、尖点/奇异曲线和一般有理代数曲线应保留回归。

当前使用用户提供的36道题作为主验收集。checkpoint 后只允许新建一个出题代理，根据剩余错题出变体；主代理负责修复和验收，交付后结束该出题代理，不复用历史上下文。

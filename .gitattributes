# Preserve the exact bytes of the pinned upstream snapshot.
* -text
# Recognize inherited CRLF line endings while checking real trailing whitespace.
src/** whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
